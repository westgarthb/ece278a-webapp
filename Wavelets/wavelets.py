# Author: Jackie

# Dependencies
from matplotlib.transforms import Transform
import numpy as np
import matplotlib.pyplot as plt
import streamlit as st

############################

# TODO: Continuous
def continous():

    st.subheader('Continuous Wavelets')

    st.write('Definition of contious Wavelets')

    # Formula for CWT
    st.latex(r'''T\left(a, b\right) = ''')
    #st.latex(r'''g\left(i, j\right) = \sum_{k,l} f(k, l)h(i - rk, j - rl)''')

# TODO: Discrete
def discrete():
    st.subheader('Discrete Wavelets')

    st.write('Definition of Discrete Wavelets')

    # Formula for CWT
    st.latex(r'''T\left(m n\right) = ''')
    #st.latex(r'''g\left(i, j\right) = \sum_{k,l} f(k, l)h(i - rk, j - rl)''')


# TODO: Applition Tool
def wavelets_app():
    TODO: 
